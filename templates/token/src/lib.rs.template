#![no_std]
use soroban_sdk::{contract, contractimpl, Env};

#[contract]
pub struct TokenContract;

#[contractimpl]
impl TokenContract {
    // TODO: Implement token interface (initialize, mint, burn, transfer, etc.)
    pub fn initialize(_env: Env) {
        unimplemented!();
    }
}

#[cfg(test)]
mod test;
